---
import { app_name } from '../../common/const'
import Icon_github from './icon_github.astro'
---

<header>
    <h1>{app_name}</h1>
    <!-- qq group -->
    <a href='https://github.com/ppzreboot/ffend' target='_blank'>
        <Icon_github />
    </a>
</header>
<div class='header_placeholder'></div>

<style>
header {
    height: var(--header-height);
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0 1rem;
    background: rgba(var(--fc), .06);
    border-bottom: 1px solid rgba(var(--fc), .08);
}
h1 {
    font-size: 14px;
    margin: 0;
}
a {
    color: inherit;
}

@media (width >= 800px) {
    header {
        position: fixed;
        z-index: 10;
        top: 0;
        width: 100vw;
        padding-right: 2rem;
    }
    header::after {
        background-color: rgb(var(--bc));
        content: '';
        display: block;
        position: absolute;
        left: 0;
        right: 0;
        top: 0;
        bottom: 0;
        z-index: -1;
    }
    h1 {
        font-weight: normal;
    }
    .header_placeholder {
        height: var(--header-height);
        width: 10px;
    }
}
@media (width < 800px) {
    h1 {
    }
}
</style>
